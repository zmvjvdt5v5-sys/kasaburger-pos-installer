name: Build Windows Installer

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    
    defaults:
      run:
        working-directory: ./electron
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Install dependencies
      run: npm install
      
    - name: Build Windows Installer
      run: npm run build:win
      
    - name: Upload Installer Artifact
      uses: actions/upload-artifact@v4
      with:
        name: KasaBurger-POS-Windows
        path: |
          electron/dist/*.exe
        retention-days: 30
        
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v1.0.${{ github.run_number }}
        name: KasaBurger POS v1.0.${{ github.run_number }}
        body: |
          ## 🍔 KasaBurger POS Masaüstü Uygulaması
          
          ### İndirme
          - **Setup (Kurulum):** `KasaBurger.POS.Setup.1.0.0.exe` - Bayilere bu gönderilecek
          - **Portable:** `KasaBurger.POS.1.0.0.exe` - Kurulum gerektirmez
          
          ### Kurulum
          1. Setup dosyasını indirin
          2. Çift tıklayın ve kurulumu tamamlayın
          3. Masaüstü kısayolundan açın
          4. Bayi bilgilerinizle giriş yapın
        files: |
          electron/dist/*.exe
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
